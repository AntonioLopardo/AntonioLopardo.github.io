User-agent: *
# papers.html is intentionally crawlable so bots can read its noindex meta tag.
# The feed can't carry one (XML), so block it here instead.
Disallow: /assets/feeds/

Sitemap: https://AntonioLopardo.github.io/sitemap.xml
