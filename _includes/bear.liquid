<style>
  /* Bear-blog styling, scoped to .bear-blog so the rest of the site is untouched.
     Colors reuse al-folio theme variables so light/dark still follow the toggle. */
  .bear-blog {
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    font-size: 1.05rem;
    line-height: 1.65;
  }
  .bear-blog p,
  .bear-blog li {
    font-size: 1.05rem;
    line-height: 1.65;
  }
  .bear-blog h1 {
    font-size: 1.9rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.5rem;
  }
  .bear-blog h2 {
    font-size: 1.4rem;
    margin-top: 2rem;
  }
  .bear-blog h3 {
    font-size: 1.2rem;
    margin-top: 1.5rem;
  }
  .bear-blog a {
    color: var(--global-theme-color);
    text-decoration: none;
  }
  .bear-blog a:hover {
    text-decoration: underline;
  }
  .bear-blog .post-header {
    margin-bottom: 2rem;
  }
  .bear-blog .post-meta {
    color: var(--global-text-color-light);
    font-size: 0.9rem;
  }

  /* Post index: a tight date + title list */
  .bear-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .bear-list li {
    display: flex;
    align-items: baseline;
    gap: 1rem;
    margin: 0.4rem 0;
  }
  .bear-list .date {
    flex: 0 0 5.5rem;
    font-variant-numeric: tabular-nums;
    color: var(--global-text-color-light);
    font-size: 0.9rem;
    white-space: nowrap;
  }
</style>
